import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    isLogged:false,
}

const userSlicer=createSlice({
    name:'user',
    initialState,
    reducers:{
        
    }
})